{
  "name": "recipe-prototype",
  "version": "0.0.1",
  "description": "A prototype recipe block",
  "main": "build/index.js",
  "scripts": {
    "build": "wp-scripts build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "kdevnel",
  "license": "GPL-2.0-only",
  "devDependencies": {
    "@wordpress/scripts": "18.0.0"
  }
}
